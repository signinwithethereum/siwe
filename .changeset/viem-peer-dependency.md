---
'@signinwithethereum/siwe': minor
---

Add `viem` as an optional peer dependency alongside `ethers` — either library can now be used for signature verification via `createViemConfig()` or `createEthersConfig()`
