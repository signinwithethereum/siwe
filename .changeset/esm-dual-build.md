---
'@signinwithethereum/siwe-parser': major
'@signinwithethereum/siwe': minor
---

Ship dual ESM/CJS builds via Vite with proper `exports` map — packages are now `"type": "module"` with `.mjs` and `.cjs` entry points
